name: test
on:
  pull_request:
    branches:
      - main

env:
  x: "bollocks"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0 # Needed for gitversion

      - name: Replace Tokens
        uses: scott-david-walker/tokeniser@0.0.1-alpha.8
        with:
          prefix: "{{{"
          files: "**/*.yaml"
          fail-if-no-provided-replacement: false
        env:
          scott: ${{ env.x }}

      - name: output
        run: |
          cat test.txt

      - name: output2
        working-directory: fake
        run: |
          cat test.yaml

